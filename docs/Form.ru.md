[English](Form.md) | [Русский](Form.ru.md)

🚀 [Быстрый старт](../QuickStart.ru.md)

# Класс Form

## Назначение
Класс представления форм ввода данных фреймворка LOTIS, наследующий `Element`. Предназначен для создания интерактивных форм с поддержкой различных типов полей ввода, обработки событий отправки, валидации и синхронизации данных между сервером и клиентом. Класс автоматически создаёт объект `Events` для сервер-клиент коммуникации, предоставляет методы для генерации полей по конфигурации, организации полей в сетку (Cells/Grid) и управления значениями формы через клиентские методы. Интегрируется с `Input`, `LookupField`, `DataTable` для создания сложных форм ввода.

### Основные возможности

* **Создание полей** различных типов
* **Управление значениями** полей
* **Обработка событий** отправки
* **Валидация данных**
* **Организация структуры** формы

### Типы полей

Класс поддерживает создание следующих типов полей:

* **Текстовые поля** (text, email, tel, url)
* **Числовые поля** (number, range)
* **Дата и время** (date, time)
* **Специальные типы** (color, file, hidden)
* **Кнопки** (submit, reset, button)
* **Флажки и радио** (checkbox, radio)
* **Списки** (select)
* **Текстовые области** (textarea)
* **Поля поиска с подкачкой из базы данных** (LookupField)

## Свойства

| Свойство | Тип | Видимость | Описание |
|----------|-----|-----------|----------|
| **$events** | `Events` | `public` | Объект событий для сервер-клиент коммуникации. Создаётся в конструкторе с ID `"{$this->id}_events"`. Используется для обработки нажатий кнопок и отправки данных формы. |

### Наследуемые свойства

| Свойство | Тип | Видимость | Описание |
|----------|-----|-----------|----------|
| **$id** | `string` | `public` | Наследуется от `Construct`. Уникальный идентификатор формы, используется как HTML-атрибут `id` и `name`. |
| **$type** | `string` | `public` | Наследуется от `Element`. Устанавливается в `'html'`. Определяет тип объекта для фильтрации и обработки. |
| **$tagname** | `string` | `public` | Наследуется от `Element`. Устанавливается в `'form'` в конструкторе. Определяет HTML-тег элемента при рендеринге. |
| **$storage** | `array` | `public` | Наследуется от `Quark`. Хранилище содержит секции `'childs'` (потомки-поля), `'element'` (DOM-элемент), `'attr'` (HTML-атрибуты), `'clicks'` (обработчики кнопок). |

## Методы (серверная часть, PHP)

### Конструктор

| Метод | Параметры | Возвращает | Описание |
|-------|-----------|------------|----------|
| **__construct** | `string $id = ''` | — | Конструктор класса. Вызывает родительский конструктор `Element`, создаёт объект `$events` с ID `"{$this->id}_events"`, устанавливает `tagname` в `'form'`. Если `$id` не пуст — устанавливает атрибут `name` формы. |

### Методы работы с формой

| Метод | Параметры | Возвращает | Описание |
|-------|-----------|------------|----------|
| **generate** | `mixed $objects` | `$this` | Генерирует поля формы из массива конфигураций. Принимает массив или JSON-строку с описаниями полей (`name`, `caption`, `type`, `value`, `values`, `id`, `readonly` и др.). Для каждого элемента создаёт соответствующий `Input` или `LookupField`. Поддерживает fluent interface. |
| **cells** | `string $names = ''`, `int $captionscol = 1`, `int $inputscol = 2` | `Cells` | Организует поля формы в табличную структуру через объект `Cells`. Принимает список имён полей, номер колонки для подписей и номер колонки для полей ввода. Возвращает объект `Cells` для дальнейшей настройки. |
| **grid** | `array $regions`, `Grid $grid = null` | `Grid` | Размещает поля формы в сетке `Grid` по областям. Принимает массив `{areaName => [fieldNames]}`. Если `$grid` не передан — создаёт новый. Возвращает объект `Grid` с формой внутри. |

### Создание полей

| Метод | Параметры | Возвращает | Описание |
|-------|-----------|------------|----------|
| **field** | `string $name` | `Element/false` | Находит поле формы по имени атрибута `name`. Перебирает потомков типа `html`, проверяет `attr('name')` или `input->attr('name')` для `LookupField`. Возвращает объект поля или `false` если не найдено. |
| **input** | `string $type`, `string $name`, `string $caption = ''` | `Input` | Создаёт поле ввода указанного типа. Создаёт объект `Input`, вызывает `setinput()`, устанавливает `$form` ссылку, добавляет в потомки. Возвращает объект `Input`. |
| **button** | `string $name`, `string $caption = ''` | `Input` | Создаёт кнопку. Создаёт объект `Input`, вызывает `button()`, устанавливает `$form` ссылку, добавляет в потомки. Возвращает объект `Input`. |
| **table** | `string $name`, `MySqlTable $dbtable`, `string $caption = ''` | `LookupField` | Создаёт поле поиска с привязкой к таблице БД. Создаёт объект `LookupField`, устанавливает `$dbtable`, заголовки, имя поля, label. Добавляет в форму. Возвращает объект `LookupField`. |
| **select** | `string $name`, `string $caption = ''`, `mixed $values = null` | `Input` | Создаёт выпадающий список. Создаёт объект `Input`, вызывает `select()`, устанавливает `$form` ссылку, добавляет в потомки. Возвращает объект `Input`. |
| **textarea** | `string $name`, `string $caption = ''` | `Input` | Создаёт текстовую область. Создаёт объект `Input`, вызывает `textarea()`, устанавливает `$form` ссылку, добавляет в потомки. Возвращает объект `Input`. |
| **checkbox** | `string $name`, `string $caption = ''` | `Input` | Создаёт чекбокс. Создаёт объект `Input`, вызывает `checkbox()`, устанавливает `$form` ссылку, добавляет в потомки. Возвращает объект `Input`. |
| **file** | `string $name`, `string $caption = ''` | `Input` | Создаёт поле загрузки файла. Создаёт объект `Input`, вызывает `file()`, устанавливает `$form` ссылку, добавляет в потомки. Возвращает объект `Input`. |
| **hidden** | `string $name`, `string $caption = ''` | `Input` | Создаёт скрытое поле. Создаёт объект `Input`, вызывает `hidden()`, устанавливает `$form` ссылку, добавляет в потомки. Возвращает объект `Input`. |
| **image** | `string $name`, `string $caption = ''` | `Input` | Создаёт кнопку-изображение. Создаёт объект `Input`, вызывает `image()`, устанавливает `$form` ссылку, добавляет в потомки. Возвращает объект `Input`. |
| **password** | `string $name`, `string $caption = ''` | `Input` | Создаёт поле пароля. Создаёт объект `Input`, вызывает `password()`, устанавливает `$form` ссылку, добавляет в потомки. Возвращает объект `Input`. |
| **radio** | `string $name`, `string $caption = ''`, `array $values = null` | `Input` | Создаёт группу радио-кнопок. Создаёт первый объект `Input`, вызывает `radio()`, для остальных значений создаёт дополнительные `Input`. Устанавливает `$form` ссылку, добавляет в потомки. Возвращает первый объект `Input`. |
| **reset** | `string $name`, `string $caption = ''` | `Input` | Создаёт кнопку сброса формы. Создаёт объект `Input`, вызывает `reset()`, устанавливает `$form` ссылку, добавляет в потомки. Возвращает объект `Input`. |
| **submit** | `string $name`, `string $caption = ''` | `Input` | Создаёт кнопку отправки формы. Создаёт объект `Input`, вызывает `submit()`, устанавливает `$form` ссылку, добавляет в потомки. Возвращает объект `Input`. |
| **text** | `string $name`, `string $caption = ''` | `Input` | Создаёт текстовое поле. Создаёт объект `Input`, вызывает `text()`, устанавливает `$form` ссылку, добавляет в потомки. Возвращает объект `Input`. |
| **email** | `string $name`, `string $caption = ''` | `Input` | Создаёт поле email (HTML5). Создаёт объект `Input`, вызывает `email()`, устанавливает `$form` ссылку, добавляет в потомки. Возвращает объект `Input`. |
| **tel** | `string $name`, `string $caption = ''` | `Input` | Создаёт поле телефона (HTML5). Создаёт объект `Input`, вызывает `tel()`, устанавливает `$form` ссылку, добавляет в потомки. Возвращает объект `Input`. |
| **url** | `string $name`, `string $caption = ''` | `Input` | Создаёт поле URL (HTML5). Создаёт объект `Input`, вызывает `url()`, устанавливает `$form` ссылку, добавляет в потомки. Возвращает объект `Input`. |
| **date** | `string $name`, `string $caption = ''` | `Input` | Создаёт поле даты (HTML5). Создаёт объект `Input`, вызывает `date()`, устанавливает `$form` ссылку, добавляет в потомки. Возвращает объект `Input`. |
| **color** | `string $name`, `string $caption = ''` | `Input` | Создаёт поле выбора цвета (HTML5). Создаёт объект `Input`, вызывает `color()`, устанавливает `$form` ссылку, добавляет в потомки. Возвращает объект `Input`. |
| **time** | `string $name`, `string $caption = ''` | `Input` | Создаёт поле времени (HTML5). Создаёт объект `Input`, вызывает `time()`, устанавливает `$form` ссылку, добавляет в потомки. Возвращает объект `Input`. |
| **number** | `string $name`, `string $caption = ''` | `Input` | Создаёт поле числа (HTML5). Создаёт объект `Input`, вызывает `number()`, устанавливает `$form` ссылку, добавляет в потомки. Возвращает объект `Input`. |
| **range** | `string $name`, `string $caption = ''`, `int $min = 1`, `int $max = 100` | `Input` | Создаёт ползунок диапазона (HTML5). Создаёт объект `Input`, вызывает `range()` с `$min` и `$max`, устанавливает `$form` ссылку, добавляет в потомки. Возвращает объект `Input`. |

### Работа с событиями

| Метод | Параметры | Возвращает | Описание |
|-------|-----------|------------|----------|
| **getEvent** | — | `Events` | Возвращает объект событий формы. Используется для прямой регистрации обработчиков через `$form->getEvent()->server()`. |
| **eventname** | `mixed $button` | `string` | Возвращает имя события для кнопки. Принимает ID (строку) или объект кнопки, извлекает `$button->id`. Используется внутри `event()`. |
| **event** | `mixed $button`, `callable/string/false $func` | `$this` | Регистрирует обработчик нажатия кнопки. Принимает кнопку (ID или объект) и функцию/JS-код. Если `$func === false/null` — отключает событие. Если строка — добавляет JS-код. Если callable — добавляет серверный обработчик. Устанавливает флаг в секции `'clicks'`. Поддерживает fluent interface. |
| **error** | `mixed $button`, `string $body` | `$this` | Регистрирует обработчик ошибки для кнопки. Принимает кнопку (ID или объект) и JS-код обработчика. Вызывает `$events->error()`. Поддерживает fluent interface. |
| **compile** | — | — | Генерация клиентских JavaScript-методов. Если есть обработчики кнопок в `'clicks'` — генерирует код для каждого: создаёт метод `value(name, val)`, `values(vals)`, `data()`, `clear()`. Для кнопок с обработчиками — добавляет click-обработчик с отправкой данных через `LTS.get(formId).data()`. Вызывает `parent::compile()`. |

## Методы (клиентская часть, генерируются при compile)

| Метод | Параметры | Возвращает | Описание |
|-------|-----------|------------|----------|
| **LTS(form).value(name, val)** | `string name`, `mixed val` | `mixed` | Получает или устанавливает значение поля по имени. Если `val` не передан — возвращает текущее значение. Для чекбоксов возвращает `1`/`0`, для радио — значение выбранной кнопки, для `LookupField` — ID выбранной записи. |
| **LTS(form).values(vals)** | `object vals` | `object` | Получает или устанавливает значения всех полей формы. Если `vals` передан — устанавливает значения и возвращает `this`. Иначе — возвращает объект со всеми значениями полей. Для `LookupField` использует `fieldmap` для маппинга полей. |
| **LTS(form).data()** | — | `FormData` | Возвращает данные формы в формате `FormData`. Используется для отправки файлов через `Events`. Включает все поля кроме `button`, `submit`, `reset`. Для чекбоксов добавляет `1`/`0`, для радио — только выбранное значение, для `file` — файл из `input.files[0]`. |
| **LTS(form).clear()** | — | `object` | Очищает все поля формы. Вызывает `values({})` — устанавливает все значения в пустые строки, чекбоксы в `false`. |

## Объект ltsForm (клиентская часть, JS)

| Свойство | Тип | Описание |
|----------|-----|----------|
| **values** | `function` | Метод получения/установки значений формы. Возвращает объект с данными полей. |
| **data** | `function` | Метод получения данных формы в формате `FormData`. Используется для отправки файлов. |
| **clear** | `function` | Метод очистки всех полей формы. |
| **value** | `function` | Метод получения/установки значения конкретного поля по имени. |
| **normalizeInputValue** | `function` | Метод преобразования строкового значения под формат HTML5-поля указанного типа. |

## Метод normalizeInputValue (клиентская часть, JS)

| Тип поля | Формат ввода | Формат вывода | Описание |
|----------|--------------|---------------|----------|
| **date** | `YYYY-MM-DD` | `YYYY-MM-DD` | Проверяет валидность даты, возвращает `null` если некорректно. |
| **time** | `HH:MM` или `HH:MM:SS` | `HH:MM` или `HH:MM:SS` | Проверяет диапазон часов (0-23), минут (0-59), секунд (0-59). |
| **datetime-local** | `YYYY-MM-DDTHH:MM` | `YYYY-MM-DDTHH:MM` | Проверяет валидность даты и времени. |
| **month** | `YYYY-MM` | `YYYY-MM` | Проверяет диапазон месяцев (1-12). |
| **week** | `YYYY-Www` | `YYYY-Www` | Проверяет диапазон недель (1-53). |
| **number/range** | Число | Строка числа | Парсит через `parseFloat()`, возвращает `null` если `NaN`. |
| **url** | URL с протоколом | Полный URL | Если нет протокола — добавляет `http://`. Возвращает `null` если невалидно. |
| **email** | email@domain.tld | Строка email | Простая проверка на `@` и точку в домене. |
| **color** | `#RRGGBB` | `#rrggbb` | Проверяет формат HEX, приводит к нижнему регистру. |
| **text/password/search/tel/hidden** | Любая строка | Строка | Возвращает как есть после `trim()`. |

## Примеры использования

```php
// Создание формы
$myform = LTS::Form();

// Добавление полей
$myform->text('username', 'Логин')
     ->password('password', 'Пароль')
     ->email('email', 'Email');
// Добавление кнопок
$myform->button('close', 'Закрыть')->click('$(myform).hide()');
$clickbutton = $myform->button('clickme', 'Нажми меня');
// Привязка события к кнопке
$myform->event($clickbutton,
<<<JS
    alert(result);
JS
  )
  ->event($clickbutton, function($values) {
    return 'Привет, ' . $args['username'];
});
// Назначение хаков
$myform->js()->add(
<<<JS
    LTS(myform).checkclickme = function (args) {
        if(args.get('password') == '') {
          alert('Введите пароль!');
          return false;
        }
        return true;
    };
JS
);

// Организация полей в сетку Cells
$myform->cells([
    'username',
    'password',
    'email',
    'clickme, close'
]);
```

## Примечания
*   Класс расширяет `Element`, поэтому наследует все методы управления потомками, стилями, атрибутами и событиями.
*   Класс расширяет `Quark` и `Construct`, поэтому поддерживает полный жизненный цикл объектов: `compile()`, `shine()`, `childs()`, `addinspace()`, а также хуки (`$check`, `$before`, `$on` и др.).
*   Объект `$events` создаётся автоматически в конструкторе — не требует явного создания для обработки кнопок.
*   Метод `generate()` поддерживает JSON-строку или массив конфигураций — удобно для динамической генерации форм из метаданных.
*   Метод `cells()` возвращает объект `Cells` — позволяет дополнительно настроить сетку полей перед рендерингом.
*   Метод `grid()` размещает форму внутри `Grid` — полезно для сложных макетов с несколькими областями.
*   Метод `field()` ищет поля по атрибуту `name` — не по `$id` объекта `Input`.
*   Для `LookupField` метод `field()` проверяет `$child->input->attr('name')` — учитывает вложенную структуру.
*   Метод `event()` поддерживает три режима: `false/null` (отключить), строка (JS-код), callable (серверная функция).
*   Обработчики кнопок сохраняются в секции `'clicks'` — используются при компиляции для генерации JS-кода.
*   Метод `compile()` генерирует 4 клиентских метода: `value()`, `values()`, `data()`, `clear()` — доступны через `LTS.get('{id}')`.
*   Метод `data()` возвращает `FormData` — требуется для отправки файлов через `Events`.
*   Метод `values()` поддерживает маппинг полей для `LookupField` через `$lookup->fieldmap`.
*   Для чекбоксов `values()` возвращает `1`/`0` — не `true`/`false`.
*   Для радио-кнопок `values()` возвращает значение выбранной кнопки — невыбранные игнорируются.
*   Для `LookupField` `values()` устанавливает `selected` объект и значение в input через `normalizeInputValue()`.
*   Метод `normalizeInputValue()` поддерживает 10+ типов HTML5-полей — обеспечивает корректное форматирование значений.
*   При установке значения через `value()` для `date`/`time` — проверяется валидность формата.
*   Для `url` без протокола — автоматически добавляется `http://`.
*   Для `color` — значение приводится к нижнему регистру (`#aabbcc`).
*   Метод `clear()` устанавливает все поля в пустые значения — не удаляет поля из формы.
*   Кнопки `submit` и `reset` имеют стандартное поведение браузера — могут быть переопределены через `event()`.
*   Для кнопок с обработчиками — стандартное поведение отменяется, данные отправляются через `Events`.
*   Метод `error()` регистрирует обработчики ошибок через `$events->error()` — выполняются на клиенте при неудачном ответе.
*   При компиляции для кнопок генерируется click-обработчик — собирает данные через `LTS.get(formId).data()` и отправляет через `Events`.
*   Форма автоматически получает атрибут `name` равный `$id` — требуется для идентификации на клиенте.
*   Метод `table()` создаёт `LookupField` — интегрируется с системой поиска и автодополнения.
*   Для `radio` с `$values` — создаётся несколько `Input` с одинаковым `name` — браузер обеспечивает взаимное исключение.
*   Все методы создания полей возвращают объект поля — позволяет цепочечную настройку через fluent interface.
*   Метод `generate()` пропускает элементы без ключа `name` — требует явного указания имени поля.
*   Для полей с `readonly` в конфигурации — устанавливается атрибут `readonly='1'`.
*   Метод `cells()` собирает поля из `$this->get('childs')` — должны быть созданы до вызова `cells()`.
*   Метод `grid()` ищет поля через `getchild()` — поддерживает поиск по `tagname` и функции-предикату.
*   При размещении в `Grid` форма становится потомком области — `$this->owner` устанавливается в ID области.
*   Метод `getEvent()` возвращает `$events` — позволяет прямую регистрацию через `$form->getEvent()->server()`.
*   Обработчики событий выполняются в контексте формы — `$args` содержат данные всех полей.
*   Для файловых полей данные передаются через `FormData` — не через JSON.
*   Метод `values()` для `LookupField` использует `fieldmap` — сопоставляет имена полей таблицы с полями формы.
*   При очистке через `clear()` для `LookupField` — устанавливается `selected = null` и input очищается.
*   Метод `normalizeInputValue()` возвращает `null` для невалидных значений — поле остаётся пустым.
*   Для `datetime-local` поддерживаются форматы с `T` и пробелом между датой и временем.
*   Для `week` формат `YYYY-Www` — неделя от 01 до 53.
*   Для `month` формат `YYYY-MM` — месяц от 01 до 12.
*   Метод `value()` для чекбоксов устанавливает `checked` через `prop()` — не через атрибут.
*   Для радио-кнопок `value()` устанавливает `checked` на кнопку с matching значением.
*   Метод `data()` пропускает кнопки `button`, `submit`, `reset` — не включает их в `FormData`.
*   Для `file` полей `data()` добавляет первый файл из `input.files[0]`.
*   При отправке через `Events` данные автоматически сериализуются — массивы и объекты в JSON с префиксом `<JSON>`.
*   Метод `compile()` добавляет обработчики в области `'ready'` — выполняются после загрузки DOM.
*   Для кнопок без обработчиков — стандартное поведение браузера не отменяется.
*   Метод `eventname()` извлекает ID из объекта или строки — универсальный интерфейс.
*   При отключении события через `event($button, false)` — обработчик удаляется из `'clicks'`.
*   Метод `generate()` устанавливает дополнительные атрибуты через цикл — все ключи кроме фиксированных.
*   Фиксированные ключи конфигурации: `id`, `type`, `name`, `caption`, `values`, `value`, `min`, `max`, `class`, `dbtable`.
*   Для полей с `class` в конфигурации — вызывается `addclass()` после создания.
*   Метод `cells()` создаёт строки с классами `Row_{inputnames}` — для стилизации через CSS.
*   При организации в `Cells` label и input размещаются в разных колонках — контролируется через `$captionscol` и `$inputscol`.
*   Метод `grid()` добавляет форму в `Grid` после заполнения областей — `$grid->add($this)`.
*   Для `LookupField` в `grid()` — поиск через функцию-предикат с проверкой `hasclass('lookup-field')`.
*   Метод `field()` возвращает `false` если поле не найдено — требуется проверка перед использованием.
*   Все методы создания полей устанавливают `$input->form = $this` — связь с формой для клиентских методов.
*   При компиляции проверяется наличие обработчиков в `'clicks'` — только для них генерируется JS-код.
*   Для кнопок `submit`/`reset` метод `compile()` генерирует `click()` метод — позволяет назначать обработчики через Events.
*   Метод `value()` на клиенте ищет форму через `closest('form')` — работает только если Input внутри Form.
*   Для `LookupField` `values()` устанавливает `selected` объект — используется при сохранении строки.
*   Метод `normalizeInputValue()` использует `structuredClone()` если доступен — для глубокого копирования объектов.
*   При ошибке валидации в `normalizeInputValue()` — возвращается `null` и поле остаётся пустым.
*   Для `email` простая проверка через RegExp — не полная валидация по RFC.
*   Для `url` проверка через `new URL()` — требует современный браузер.
*   Метод `data()` для чекбоксов добавляет `1`/`0` — не `true`/`false` для совместимости с PHP.
*   При отправке файлов через `Events` — используется `FormData` с автоматическим `Content-Type`.
*   Метод `values()` мутирует исходные объекты строк — не создаёт копии при обновлении.
*   Для `LookupField` при установке значения — вызывается `trigger('change')` для уведомления других компонентов.
*   Метод `clear()` для `LookupField` — вызывает `select(null)` и очищает input.
*   При генерации через `generate()` — поля добавляются в порядке следования в массиве конфигурации.
*   Метод `cells()` переписывает `$this->storage['childs']` — добавляет `Cells` перед первым полем.
*   Для пустого `$names` в `cells()` — используются все найденные поля формы.
*   Метод `grid()` возвращает `$grid` — позволяет цепочечную настройку сетки после добавления формы.
*   При размещении в `Grid` форма наследует стили контейнера — может быть переопределено через `css()`.
*   Метод `event()` для callable — добавляет серверный обработчик через `$events->add()`.
*   Для строкового `$func` в `event()` — добавляет JS-код через `$events->js()->add()`.
*   Метод `error()` оборачивает код в `function (error) { ... }` если не начинается с `function`.
*   При компиляции для кнопок генерируется код сбора данных — `let values = LTS.get('{id}').data()`.
*   Данные кнопок отправляются через `LTS.get('{events->id}').{buttonid}(values)` — вызов события.
*   Метод `compile()` вызывает `parent::compile()` в конце — обеспечивает регистрацию в пространстве имён.
*   Для форм без кнопок с обработчиками — клиентские методы всё равно генерируются.
*   Метод `value()` на клиенте возвращает пустую строку если форма не найдена — требуется проверка.
*   Для `LookupField` в `values()` — если `val` объект — устанавливается `selected`, если строка — ищется в `fieldmap`.
*   Метод `normalizeInputValue()` для неизвестных типов — возвращает строку как есть.
*   При валидации дат — проверяется соответствие дня, месяца, года через `Date` объект.
*   Для `time` без секунд — возвращается формат `HH:MM`, с секундами — `HH:MM:SS`.
*   Метод `data()` для `radio` — добавляет только выбранное значение, остальные игнорируются.
*   При отправке через `Events` — данные автоматически включают `__ltsVars` для синхронизации глобальных переменных.
*   Метод `values()` для массива строк — обновляет все строки с созданием новых объектов.
*   Для `LookupField` при очистке — `selected` устанавливается в `null` и input очищается.
*   Метод `normalizeInputValue()` trim() входную строку — удаляет пробелы по краям.
*   При ошибке парсинга — возвращается `null` и поле остаётся без значения.
*   Для `color` проверка через RegExp `/^#[0-9A-Fa-f]{6}$/` — только 6-значный HEX.
*   Метод `value()` для чекбоксов использует `prop('checked')` — не `attr('checked')`.
*   Для радио-кнопок `value()` перебирает все кнопки группы — устанавливает `checked` на matching.
*   Метод `data()` создаёт новый `FormData` — не мутирует существующие данные.
*   При компиляции методы добавляются в JS-объект формы — доступны через `LTS.get('{id}')`.
*   Для форм с множественными кнопками — каждая кнопка имеет независимый обработчик.
*   Метод `event()` с `false` — удаляет обработчик из `'clicks'` и устанавливает `false` в `$events`.
*   При генерации через `generate()` — для `table` типа требуется ключ `dbtable` в конфигурации.
*   Метод `cells()` для объединённых полей — создаёт массив `$_arr` и новое имя через запятую.
*   Для `grid()` области заполняются через `$areaContainer->add($input)` — поля становятся потомками области.
*   Метод `field()` для `LookupField` — проверяет `$child->hasclass('lookup-field')` и `$child->input->attr('name')`.
*   Все методы создания полей добавляют поле в потомки через `$this->add($input)`.
*   При компиляции проверяется `$clicks !== false` — только если есть обработчики генерируется код.
*   Для кнопок без ID — используется `"{$this->id}_{$buttonid}"` для поиска.
*   Метод `compile()` для кнопок ищет через `child()`, `getchilds()` — несколько стратегий поиска.
*   При отправке данных — имя события равно `$buttonid` — должно совпадать с серверным обработчиком.
*   Метод `value()` на клиенте интегрируется с `ltsForm.value()` — единый интерфейс для всех форм.
*   Для `LookupField` в `values()` — если `val` пустой — `selected` устанавливается в `null`.
*   Метод `normalizeInputValue()` для `number` — использует `parseFloat()` и `isFinite()`.
*   При валидации `url` — если нет протокола — пробуем добавить `http://` и проверить снова.
*   Для `email` проверка `/^[^@\s]+@[^@\s]+\.[^@\s]+$/` — базовая валидация формата.
*   Метод `data()` для `file` — проверяет `el[0].files[0]` — добавляет только если файл выбран.
*   При компиляции JS-методы генерируются через `compilemethod()` — поддерживают хуки если включены.
*   Для форм без `id` — клиентские методы используют автоматический ID из `$this->id`.
*   Метод `values()` возвращает объект данных — может использоваться для отправки через `Events`.
*   Для `LookupField` при установке — вызывается `el.val()` с `normalizeInputValue()` для форматирования.
*   Метод `clear()` вызывает `values({})` — устанавливает все поля в пустые значения.
*   При генерации через `generate()` — для `readonly` полей устанавливается атрибут `readonly='1'`.
*   Метод `cells()` для пустых полей — создаёт ячейку но не добавляет контент.
*   Для `grid()` если поле не найдено — пропускается без ошибки.
*   Метод `field()` возвращает первый найденный элемент — при дубликатах имён behaviour не определён.
*   Все методы создания полей возвращают объект — позволяет настройку после создания.
*   При компиляции код кнопок добавляется в область `'ready'` — выполняется после загрузки DOM.
*   Для кнопок с обработчиками — стандартная отправка формы отменяется через JS.
*   Метод `event()` для строки — код добавляется через `$events->js()->add()` без обёртки.
*   При ошибке в `normalizeInputValue()` — поле остаётся с предыдущим значением.
*   Для `datetime-local` поддерживаются разделители `T` и пробел — парсятся оба варианта.
*   Метод `data()` для чекбоксов использует `el.is(':checked')` — проверяет текущее состояние.
*   При отправке через `Events` — файлы передаются как есть, остальные данные сериализуются.
*   Метод `values()` для обновления — мутирует объекты строк через `Object.assign()`.
*   Для `LookupField` в `values()` — если `selected` есть — берётся `selected['id']` для формы.
*   Метод `normalizeInputValue()` для `null` — возвращает `null` без обработки.
*   При валидации `week` — неделя проверяется в диапазоне 1-53.
*   Для `month` — месяц проверяется в диапазоне 1-12.
*   Метод `value()` для остальных типов — использует `el.val()` для получения/установки.
*   При компиляции для кнопок генерируется код — `LTS.get('{button->id}').click()`.
*   Для форм с `id` — атрибут `name` устанавливается равным `$id` в конструкторе.
*   Метод `generate()` для неизвестных типов — создаёт `text` input по умолчанию.
